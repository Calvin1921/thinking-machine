node_modules/
dist/
*.log
.gstack/
*.tsbuildinfo
coverage/

# secrets — never commit real env files
.env*
!.env.example

# runtime board data + smoke artifacts
apps/web/board.json
apps/web/boards/
boards/
board.json
docs/smoke-*.png

.DS_Store

# local canvas captures
docs/broken*.png
docs/fixed-*.png

# process scratch (implementation plans / superseded specs) — not for the tracked repo;
# the current design record lives in docs/*.md (see docs/README.md)
docs/superpowers/
